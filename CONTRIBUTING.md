# CONTRIBUTING.md —— 代码风格与提交规范

本工程以 Google C++ Style Guide 为基础，按我们的实际情况做了删减和补充。
原则只有一条：**代码是写给人看的，风格一致比个人喜好重要。**

## 命名

| 对象 | 风格 | 例子 |
|---|---|---|
| 文件名 | 小写 + 下划线 | `driver_loader.cpp`、`technique.hpp` |
| 类型（类/结构/枚举） | 大驼峰 | `TechniqueDescriptor`、`IntelNalProvider` |
| 函数 | 大驼峰（用户态）/ 大驼峰（内核） | `LoadDriver()`、`HkNtClose()` |
| 局部变量/参数 | 小写下划线 | `driver_path`、`tech_mask` |
| 类成员 | 小写下划线 + 尾下划线 | `device_handle_` |
| 常量/宏 | 全大写（宏）/ k 前缀（常量） | `ADBG_ABI_VERSION`、`kMaxTargets` |
| 命名空间 | 小写单词 | `adbg`、`kdm` |

## 格式

- 缩进 4 空格，不用 Tab。
- 一行尽量不超过 100 列。
- 大括号风格跟随各文件现状（内核文件保持 WDK 习惯的大括号换行）。

## 内核驱动的特殊约束（重要）

antidebug.sys 不是正常加载的驱动，而是被"手动映射"进内核的（原理见
`antidebug/docs/02-architecture.md` 第 6 节）。这意味着没有加载器帮它做初始化，
所以：

1. **禁用 CRT**（不链接、不使用）：不能用 `new/delete`、`std::string`、
   `memcpy` 要用 `RtlCopyMemory`。
2. **禁用 C++ 异常和 RTTI**（编译选项 `/EHs-c- /GR-`）：不用 `try/throw/dynamic_cast`。
   注意：`__try/__except`（结构化异常，SEH）在钩子处理函数里允许使用，
   因为那是用来安全地碰用户内存的，性质不同。
3. **禁用栈保护**（`/GS-`）：映射器不支持。
4. **全局变量必须零初始化**：没有 CRT 帮忙跑构造函数。指针初始化用 `= nullptr`，
   不要用带构造函数的全局对象。
5. **入口是 `CustomDriverEntry`**，不是 `DriverEntry`；参数也不是
   DRIVER_OBJECT，而是（启动邮箱指针，魔数）。详见 docs/03。
6. 钩子处理函数是热路径（每次系统调用都可能进来），里面**不加锁、不分配内存、
   不打印日志**（调试日志用编译开关控制，默认关）。

## 文档写作规范

- 正文中文，必要的英文术语保留原文，第一次出现时用一句话解释。
- 用平实的词。判断标准：新同事不用来问你"这句什么意思"。
- 每篇文档开头有统一的信息块：状态 / 日期 / 对应代码版本。
- 结构参考 Google 的设计文档习惯：背景 → 目标与非目标 → 方案 → 备选方案 → 风险。

## 提交规范

- 一个提交做一件事。文档、重构、功能分开提交。
- 提交说明第一行：中文短句 + 里程碑前缀，如 `M1: 技术注册表框架落地`。
- 动了协议（adbg_abi.h）的提交，说明里必须写清协议版本是否变化。

## 第三方代码

- 移植第三方代码时，在文件头部保留原始版权与许可声明，并在
  `antidebug/docs/06-security-and-ops.md` 的「代码来源与许可」表格里登记。
- 禁止把 iqvw64e.sys 的真实字节提交进仓库（包括生成出来的头文件）。
